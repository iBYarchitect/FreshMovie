import SwiftUI

extension Color {
    enum App {
        static let grayLight = Color(red: 239 / 255, green: 239 / 255, blue: 239 / 255)
        static let grayDark = Color(red: 126 / 255, green: 126 / 255, blue: 126 / 255)
    }
}
